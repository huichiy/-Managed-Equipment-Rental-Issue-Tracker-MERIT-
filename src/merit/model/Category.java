package merit.model;

/**
 * Equipment category. Drives which {@code Equipment} subclass is reconstructed
 * from a flat database row and which default {@code PenaltyPolicy} applies.
 */
public enum Category {
    ELECTRONICS,
    MEDIA,
    LAB
}
